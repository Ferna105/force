'use strict';

/**
 * house-placement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::house-placement.house-placement');
